import decoratorsFetchProp from 'frontend-freddie/utils/decorators/fetch-prop';
import { module, test } from 'qunit';

module('Unit | Utility | decorators/fetch-prop', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = decoratorsFetchProp();
    assert.ok(result);
  });
});
