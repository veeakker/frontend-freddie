import transitionsCrossFade from 'frontend-freddie/utils/transitions/cross-fade';
import { module, test } from 'qunit';

module('Unit | Utility | transitions/cross-fade', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = transitionsCrossFade();
    assert.ok(result);
  });
});
